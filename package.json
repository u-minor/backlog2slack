{
  "name": "backlog2slack",
  "version": "1.0.0",
  "private": true,
  "description": "Send Backlog notification to Slack",
  "author": "Minoru Nakata <minoru@sprocket.bz>",
  "license": "ISC",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "clean": "rm -rf build dist",
    "build": "tsc",
    "lambda": "npm run build && mkdir -p dist && cp -r package*.json build && pushd build && npm install --omit=dev && rm package*.json && zip -r ../dist/lambda.zip . > /dev/null && popd",
    "eslint": "eslint --ext .js .",
    "lint": "run-p -l prettier tsc eslint",
    "prettier": "prettier --log-level warn -c .",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "lodash": "^4.17.15",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.8",
    "slack-node": "^0.1.8"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "6.4.0",
    "@typescript-eslint/parser": "6.4.0",
    "eslint": "8.47.0",
    "eslint-config-prettier": "9.0.0",
    "eslint-config-standard": "17.1.0",
    "npm-run-all": "4.1.5",
    "prettier": "3.0.2",
    "typescript": "5.1.6"
  },
  "eslintConfig": {
    "extends": [
      "eslint:recommended",
      "plugin:@typescript-eslint/recommended",
      "prettier"
    ]
  },
  "eslintIgnore": [
    "build"
  ],
  "prettier": {
    "singleQuote": true
  }
}
